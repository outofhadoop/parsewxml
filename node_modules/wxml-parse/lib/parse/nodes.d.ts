import { NODE_TYPES, BaseNode } from "./type";
export declare type RootNode = {
    type: NODE_TYPES;
    childNodes: BaseNode[];
};
export declare class CommentNode extends BaseNode {
    comment: string;
    constructor(comment: string);
    toJSON(): {
        type: NODE_TYPES;
        comment: string;
    };
}
export declare class TextNode extends BaseNode {
    textContent: string;
    constructor(text: string, parentNode: any);
    toJSON(): {
        type: NODE_TYPES;
        textContent: string;
    };
}
export declare class ElementNode extends BaseNode {
    tagName: string;
    attributes: {
        [attrName: string]: string | true;
    };
    childNodes: Array<ElementNode | TextNode | CommentNode>;
    selfClosing: boolean;
    closing: boolean;
    constructor({ tagName }?: {
        tagName?: string;
    });
    toJSON(): Object;
}
